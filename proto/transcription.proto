syntax = "proto3";

package transcriptions;


message TranscriptionRequest {
    bytes audio_data = 1; // The audio data as binary
}


message TranscriptionResponse {
    string transcription = 1; // The transcribed text
}
